export const user = {
      create: jest.fn(),
      findUnique: jest.fn(),
      findMany: jest.fn(),
};
export const spam = {
      findUnique: jest.fn(),
};
export const contact = {
      findMany: jest.fn(),
};
